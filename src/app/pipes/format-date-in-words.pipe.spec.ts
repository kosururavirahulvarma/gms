import { FormatDateInWordsPipe } from './format-date-in-words.pipe';

describe('FormatDateInWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateInWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
